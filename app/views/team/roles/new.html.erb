<h1>Create Role</h1>
<%= form_with model: @team.team_roles.new do |form| %>
  <%= form.label :name %>
  <%= form.text_field :name, required: true, placeholder: "Name" %>
  <%= form.submit "Create" %>
<% end %>
